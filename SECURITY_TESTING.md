# การทดสอบความปลอดภัย — LearnHub

| | |
|---|---|
| ระบบที่ทดสอบ | https://learnhub2.vercel.app (Vercel + TiDB Cloud) |
| วันที่ทดสอบล่าสุด | 15 สิงหาคม 2569 |
| ผลล่าสุด | **ผ่าน 20/20 กรณีทดสอบ · ไม่มีข้อวิกฤตค้าง** |
| เครื่องมือ | `scripts/security-test.mjs` (เขียนขึ้นเองสำหรับระบบนี้) |
| ขอบเขต | เว็บแอปพลิเคชันและ API ทั้งหมด ไม่รวมโครงสร้างพื้นฐานของผู้ให้บริการ |

---

## 1. วิธีการทดสอบ

### 1.1 หลักการที่ยึด

**ทดสอบกับระบบที่ใช้งานจริง ไม่ใช่กับสภาพแวดล้อมจำลอง** เพราะความแตกต่างของการตั้งค่าระหว่างเครื่องพัฒนากับ production คือจุดที่ช่องโหว่มักหลุดรอด ตัวอย่างจริงในโครงการนี้: security header ถูกตั้งที่ชั้น Next.js ซึ่งจะไม่มีผลถ้าดูแต่โค้ด ต้องยิงคำขอจริงเข้าไปดูจึงรู้ว่ายังไม่ได้ตั้ง

**ทดสอบให้ล้มเหลวได้ ไม่ใช่ทดสอบให้ผ่าน** ทุกกรณีเขียนในรูป "สิ่งนี้ต้องถูกปฏิเสธ" แล้วลองทำจริง ถ้าระบบยอมให้ทำได้คือไม่ผ่าน

**ทำให้รันซ้ำได้** ชุดทดสอบเป็นสคริปต์เดียวจบ รันได้ทั้งกับ production และเครื่องตัวเอง เพื่อยืนยันว่าช่องโหว่ที่แก้ไปแล้วไม่กลับมาอีกจากการแก้โค้ดครั้งหลัง

**ไม่ทำลายข้อมูล** ทุกกรณีเป็นการอ่านหรือการเขียนที่ย้อนกลับได้ ใช้เฉพาะบัญชีเดโม (`@learnhub.demo`) ไม่แตะข้อมูลผู้ใช้จริง

### 1.2 วิธีรัน

```bash
node scripts/security-test.mjs                        # ทดสอบ production
node scripts/security-test.mjs http://localhost:3111  # ทดสอบเครื่องตัวเอง
```

สคริปต์คืน exit code เป็น `1` เมื่อพบข้อระดับวิกฤต จึงใช้เป็นด่านกั้นก่อน deploy ได้ และเขียนผลลัพธ์เป็น `security-test-results.json` สำหรับแนบในรายงาน

### 1.3 กรอบที่ใช้เลือกหัวข้อทดสอบ

เลือกจาก **OWASP Top 10** เฉพาะข้อที่เกี่ยวกับระบบนี้จริง ตัดข้อที่ไม่มีในสถาปัตยกรรมออก (เช่น XXE เพราะไม่มีการรับ XML)

| OWASP | หมวดในชุดทดสอบ |
|---|---|
| A01 Broken Access Control | A. สิทธิ์ (7 กรณี) |
| A02 Cryptographic Failures | D. ความลับ (3 กรณี) |
| A03 Injection | C. Injection (3 กรณี) |
| A04 Insecure Design | B. ตรรกะการสอบและการเงิน (4 กรณี) |
| A05 Security Misconfiguration | E. โครงสร้างพื้นฐาน (3 กรณี) |
| A07 Identification & Authentication Failures | A1, E3 |

---

## 2. กรณีทดสอบและผลล่าสุด

### A. การยืนยันตัวตนและสิทธิ์

| รหัส | สิ่งที่ทดสอบ | วิธีทดสอบ | ผล |
|---|---|---|---|
| A1 | เรียก API ผู้ดูแลโดยไม่ล็อกอิน | `GET /api/admin/students` ไม่มีคุกกี้ | ✅ 401 |
| A2 | ปลอมคุกกี้ `userRole=admin` | ส่งคุกกี้ที่สร้างเอง | ✅ 401 |
| A3 | บัญชีผู้เรียนเข้า API ผู้ดูแล | ล็อกอินจริงแล้วเรียก | ✅ 403 |
| A4 | ผู้เรียนยกระดับตัวเอง | เซสชันจริง + คุกกี้ `userRole=admin` | ✅ 403 |
| A5 | ผู้เรียนอ่านค่าตั้งที่เก็บ API key | เซสชันจริง + คุกกี้ปลอม | ✅ 403 |
| A6 | ผู้เรียนเห็นข้อมูลของคนอื่น (IDOR) | เทียบรายการลงทะเบียนของสองบัญชี | ✅ ไม่ซ้ำกัน |
| A7 | ผู้เรียนอนุมัติการชำระเงินให้ตัวเอง | `PATCH /api/admin/enrollments/:id` | ✅ 403 |

### B. ความถูกต้องของการสอบและการเงิน

| รหัส | สิ่งที่ทดสอบ | วิธีทดสอบ | ผล |
|---|---|---|---|
| B1 | ไคลเอนต์อ้างผลการตรวจเอง | ส่งคำตอบผิดพร้อมฟิลด์ `isCorrect: true` | ✅ เซิร์ฟเวอร์ตรวจเอง |
| B2 | เฉลยรั่วไปกับข้อสอบ | ตรวจ payload ของ `/api/question` | ✅ ไม่มีเฉลย |
| B3 | แก้ยอดเงินที่ต้องจ่าย | ส่ง `amount: 1` ตอนลงทะเบียนคอร์สราคาเต็ม | ✅ อ่านราคาจากฐานข้อมูล |
| B4 | ดูวิดีโอโดยยังไม่ได้รับอนุมัติ | เรียกรายละเอียดคอร์สที่ยังไม่จ่าย | ✅ ไม่ส่ง URL วิดีโอ |

### C. การแทรกคำสั่งและสคริปต์

| รหัส | สิ่งที่ทดสอบ | วิธีทดสอบ | ผล |
|---|---|---|---|
| C1 | SQL injection ที่หน้าเข้าสู่ระบบ | 4 payload เช่น `' OR '1'='1`, `'; DROP TABLE User;--` | ✅ ไม่สำเร็จ |
| C2 | XSS ที่ข้อความจากผู้ใช้ | ใส่ `<script>` ในหมายเหตุการโอน | ✅ เก็บเป็นข้อความ |
| C3 | พารามิเตอร์แปลกปลอม | ส่ง SQL ในพารามิเตอร์ `subject` | ✅ 400 |

> **หมายเหตุ** ระบบใช้ Prisma ORM ทั้งหมด ไม่มี raw SQL แม้แต่จุดเดียว การค้นหาถูกส่งเป็นพารามิเตอร์แยกจากคำสั่งเสมอ SQL injection จึงถูกปิดโดยโครงสร้าง ไม่ใช่ด้วยการกรองข้อความ ส่วน XSS ถูกปิดด้วย React ที่หนีอักขระพิเศษให้อัตโนมัติ และระบบไม่ใช้ `dangerouslySetInnerHTML` เลย

### D. การจัดการความลับ

| รหัส | สิ่งที่ทดสอบ | วิธีทดสอบ | ผล |
|---|---|---|---|
| D1 | ไฟล์ `.env` ถูก git ติดตาม | `git ls-files` | ✅ ไม่ถูกติดตาม |
| D2 | คีย์หรือรหัสผ่านฝังในซอร์สโค้ด | สแกนเนื้อไฟล์ทุกไฟล์ที่ git ติดตาม | ✅ ไม่พบ |
| D3 | API ส่งคีย์เต็มกลับไปที่เบราว์เซอร์ | เรียก `/api/admin/settings` ด้วยสิทธิ์ผู้ดูแล | ✅ ส่งเฉพาะรูปแบบปิดบัง |

### E. โครงสร้างพื้นฐานและการตั้งค่า

| รหัส | สิ่งที่ทดสอบ | วิธีทดสอบ | ผล |
|---|---|---|---|
| E1 | บังคับใช้ HTTPS | เรียกผ่าน `http://` แล้วดูการเปลี่ยนเส้นทาง | ✅ redirect |
| E2 | Security headers | ตรวจ header ของคำขอจริง | ✅ ครบ 5 รายการ |
| E3 | จำกัดการเดารหัสผ่าน | ล็อกอินผิด 12 ครั้งติดกัน | ✅ ถูกบล็อกที่ครั้งที่ 6 |

---

## 3. ช่องโหว่ที่พบและแก้ไขแล้ว

ทั้งหมดนี้พบจากการทดสอบจริงในวันเดียวกัน ไม่ใช่การคาดเดา

### 3.1 🔴 วิกฤต — รหัสผ่านฐานข้อมูล production หลุดในที่เก็บโค้ดสาธารณะ

**พบได้อย่างไร** ตัวสแกนความลับใน `scripts/package-source.mjs` หยุดการสร้างแพ็กเกจแล้วชี้มาที่ `VERCEL_ENV_SETUP.md` บรรทัด 22

**สิ่งที่พบ** สตริงเชื่อมต่อ TiDB ตัวจริงพร้อมชื่อผู้ใช้และรหัสผ่าน อยู่ในคอมมิต `d927a71` ซึ่ง push ขึ้น `origin/main` แล้ว และที่เก็บโค้ดเป็นสาธารณะ (`private: false`)

**ผลกระทบ** ใครก็ตามที่เปิดที่เก็บโค้ดสามารถเชื่อมต่อฐานข้อมูลได้โดยตรง อ่าน แก้ไข หรือลบข้อมูลผู้เรียนทั้งหมดพร้อมประวัติการชำระเงิน โดยไม่ต้องผ่านเว็บแอปพลิเคชันและไม่ต้องยืนยันตัวตนใด ๆ

**การแก้ไข**
1. ลบค่าออกจากไฟล์และแทนด้วย placeholder
2. **หมุนรหัสผ่านใหม่ที่ TiDB Cloud** — ขั้นตอนนี้คือการแก้จริง เพราะการลบออกจากไฟล์ไม่ได้ลบออกจากประวัติ git ที่เผยแพร่ไปแล้ว
3. อัปเดต `DATABASE_URL` ที่ Vercel และไฟล์สภาพแวดล้อมในเครื่อง

**การยืนยัน** ฐานข้อมูลปฏิเสธรหัสเดิม: `Authentication failed ... credentials for 2scrrMY3sMcRgAq.root are not valid` และระบบ production ยังทำงานปกติด้วยรหัสใหม่

**บทเรียน** ตัวสแกนถูกเขียนขึ้นเพื่อกันความลับหลุดออกไป**ในอนาคต** แต่กลับพบความลับที่หลุดไป**แล้ว**ตั้งแต่การรันครั้งแรก การตรวจอัตโนมัติจึงคุ้มค่าแม้กับสิ่งที่คิดว่าไม่มีปัญหา

### 3.2 🔴 วิกฤต — ปลอมสิทธิ์ผู้ดูแลได้ด้วยคุกกี้บรรทัดเดียว

**สิ่งที่พบ** `requireAdmin` ตรวจสิทธิ์จากค่าคุกกี้ `userRole` ซึ่งเป็นคุกกี้ธรรมดาที่ไม่ได้เซ็นชื่อและอยู่ในการควบคุมของฝั่งผู้ใช้ทั้งหมด

```
curl .../api/admin/students -H "Cookie: userRole=admin"   → HTTP 200
curl .../api/admin/students                                → HTTP 403
```

**ผลกระทบ** ใครก็ตามที่พิมพ์ `document.cookie='userRole=admin'` ในเบราว์เซอร์ได้สิทธิ์ผู้ดูแลเต็มรูปแบบ โดยไม่ต้องมีบัญชี ไม่ต้องรู้รหัสผ่าน — เห็นอีเมลผู้เรียนทุกคน ประวัติการชำระเงิน อนุมัติหรือปฏิเสธการจ่ายเงิน และลบคอร์สได้

**การแก้ไข** อ่าน `userId` จากคุกกี้แล้วสอบถามฐานข้อมูลว่าบัญชีนั้นมีบทบาทเป็นผู้ดูแลจริงหรือไม่ ค่าที่ปลอมได้จึงเหลือเพียง `userId` ซึ่งไม่มีความหมายหากบัญชีนั้นไม่ใช่ผู้ดูแล ปรับจุดเรียกใช้ทั้ง 16 จุดให้รอผลการตรวจ

**การตัดสินใจเพิ่มเติม** หากการสอบถามฐานข้อมูลล้มเหลว ระบบจะ**ปฏิเสธ** (503) ไม่ใช่ปล่อยผ่าน — การตรวจสิทธิ์ที่ล้มเหลวแล้วอนุญาตเป็นช่องโหว่ที่ร้ายแรงกว่าการที่ระบบใช้งานไม่ได้ชั่วคราว

**การยืนยัน** กรณี A1–A5 ผ่านทั้งหมด รวมถึงกรณีที่ผู้เรียนล็อกอินจริงแล้วเติมคุกกี้ปลอม ซึ่งเป็นสถานการณ์ที่ใกล้เคียงการโจมตีจริงที่สุด และกรณี A6–A7 ยืนยันว่าผู้ใช้ทั่วไปยังทำงานได้ตามปกติ ไม่ได้แก้จนของเดิมพัง

### 3.3 🟡 กลาง — เฉลยข้อสอบถูกส่งไปพร้อมโจทย์

**สิ่งที่พบ** `/api/question` ส่งตัวเลือกพร้อมธง `isCorrect` ของแต่ละตัว และส่งคำอธิบายเฉลยมาด้วย ผู้สอบที่เปิดแท็บ Network อ่านเฉลยได้ทุกข้อก่อนตอบ

**ทำไมจึงไม่ถูกพบมาก่อน** เซิร์ฟเวอร์ตรวจคำตอบเองอยู่แล้ว การปลอมคะแนนจึงทำไม่ได้ ปัญหานี้จึงไม่เคยแสดงออกมาเป็นบั๊กของคะแนน แต่ทำให้**ระดับที่วัดได้ไม่มีความหมาย**สำหรับผู้ที่เปิดดู ซึ่งกระทบต่อคุณค่าหลักของแพลตฟอร์มโดยตรง

**การแก้ไข** ตัดทั้ง `isCorrect` และ `explanation` ออกก่อนส่ง การตรวจคำตอบเปรียบเทียบด้วยข้อความที่ฝั่งเซิร์ฟเวอร์ จึงไม่เคยต้องใช้ธงนี้ที่ฝั่งไคลเอนต์อยู่แล้ว

### 3.4 🟠 สูง — ไม่มีการจำกัดการเดารหัสผ่าน

**สิ่งที่พบ** ส่งคำขอเข้าสู่ระบบผิด 12 ครั้งติดกันโดยไม่มีการตอบสนองใด ๆ เปิดทางให้เดารหัสผ่านด้วยเครื่องมืออัตโนมัติ ซึ่งอันตรายเป็นพิเศษเมื่อรวมกับจุดอ่อนของการเข้ารหัสรหัสผ่านในข้อ 4.1

**การแก้ไข** นับความล้มเหลวใน**ฐานข้อมูล** ไม่ใช่หน่วยความจำ เพราะบนแพลตฟอร์มแบบไร้เซิร์ฟเวอร์ คำขอที่ต่อเนื่องกันอาจถูกส่งไปคนละอินสแตนซ์ ตัวนับในหน่วยความจำจึงมองไม่เห็นกันและกันและกันอะไรไม่ได้จริง ผิด 5 ครั้งใน 15 นาทีจะได้ HTTP 429 และล็อกอินสำเร็จจะล้างประวัติทิ้ง เจ้าของบัญชีตัวจริงจึงไม่ถูกล็อกเพราะพิมพ์ผิดก่อนหน้า

**การตัดสินใจเพิ่มเติม** ตัวจำกัดอัตรา**ปล่อยผ่าน**หากตารางบันทึกของตัวเองมีปัญหา — การล็อกผู้ใช้ทุกคนออกจากระบบเพราะตารางบันทึกพังนั้นเสียหายมากกว่าการปล่อยให้เดารหัสผ่านต่อได้ชั่วคราว ซึ่งยังมีรหัสผ่านเป็นด่านอยู่

**เพิ่มเติม** ข้อความตอบกลับกรณีไม่มีบัญชีและกรณีรหัสผ่านผิดถูกทำให้เหมือนกัน เพื่อไม่ให้ใช้หน้านี้ไล่ตรวจว่าอีเมลใดมีอยู่ในระบบ

### 3.5 🟡 กลาง — ไม่มี security headers

**การแก้ไข** เพิ่มที่ `next.config.js` ครบ 5 รายการ

| Header | ป้องกันอะไร |
|---|---|
| `X-Frame-Options: DENY` | เว็บอื่นฝังหน้าเราใน iframe เพื่อหลอกให้ผู้ใช้กด (clickjacking) |
| `X-Content-Type-Options: nosniff` | เบราว์เซอร์เดาชนิดไฟล์เองจนไฟล์ถูกรันเป็นสคริปต์ |
| `Referrer-Policy` | URL เต็มของหน้าเรารั่วไปยังเว็บปลายทาง |
| `Strict-Transport-Security` | การเชื่อมต่อถูกลดระดับเป็น HTTP |
| `Permissions-Policy` | การเข้าถึงกล้อง ไมโครโฟน ตำแหน่งที่ระบบไม่ได้ใช้ |

---

## 4. ความเสี่ยงที่ยังเหลือ

รายงานอย่างตรงไปตรงมา — สิ่งเหล่านี้ยังไม่ได้แก้

### 4.1 🟠 สูง — การเข้ารหัสรหัสผ่านอ่อนกว่ามาตรฐานปัจจุบัน

```
pbkdf2Sync(password, 'salt', 1000, 64, 'sha512')
```

**ปัญหาสองข้อ**
1. **salt เป็นค่าคงที่เดียวกันทุกบัญชี** ผู้ที่ได้ฐานข้อมูลไปจึงสร้างตารางถอดรหัสครั้งเดียวใช้ได้กับทุกคน และเห็นได้ทันทีว่าใครใช้รหัสผ่านซ้ำกัน
2. **1,000 รอบ ต่ำกว่าคำแนะนำปัจจุบันมาก** OWASP แนะนำ PBKDF2-SHA512 ที่ 210,000 รอบขึ้นไป

**ทำไมยังไม่แก้** การเปลี่ยนวิธีแฮชทำให้รหัสผ่านเดิมทั้งหมดใช้ไม่ได้ ต้องทำพร้อมกลไกย้ายข้อมูลที่แฮชใหม่ตอนผู้ใช้ล็อกอินสำเร็จครั้งถัดไป เป็นงานที่ต้องวางแผนแยก

**สิ่งที่ลดความเสี่ยงไว้ชั่วคราว** การจำกัดอัตราการเข้าสู่ระบบ (ข้อ 3.4) ปิดการเดารหัสผ่านจากภายนอก จุดอ่อนนี้จึงมีผลเฉพาะกรณีฐานข้อมูลรั่ว

### 4.2 🟡 กลาง — เซสชันไม่มีวันหมดอายุฝั่งเซิร์ฟเวอร์

คุกกี้ `userId` มีอายุ 7 วันและเป็น `httpOnly` แต่เซิร์ฟเวอร์ไม่มีรายการเซสชันที่ยกเลิกได้ จึงบังคับให้ผู้ใช้ออกจากระบบทุกอุปกรณ์ไม่ได้หากสงสัยว่าบัญชีถูกยึด

**ข้อเสนอ** เพิ่มตารางเซสชันพร้อมโทเคนที่สุ่มขึ้น แทนการใช้ `userId` ตรง ๆ เป็นตัวระบุเซสชัน

### 4.3 🟡 กลาง — รหัสยืนยันการสมัครผู้ดูแลมีค่าเริ่มต้นในซอร์สโค้ด

`ADMIN_REGISTER_CODE` มีค่าเริ่มต้นเขียนอยู่ในโค้ดที่เผยแพร่สาธารณะ หากผู้ติดตั้งไม่เปลี่ยน ใครก็สมัครเป็นผู้ดูแลได้

**สถานะ** เอกสารทั้ง `USER_GUIDE.md` และ `README.md` เตือนให้เปลี่ยนแล้ว แต่ระบบยังไม่บังคับ

**ข้อเสนอ** ปฏิเสธการสมัครผู้ดูแลทั้งหมดหากตัวแปรนี้ยังเป็นค่าเริ่มต้น

### 4.4 🔵 ต่ำ — กุญแจเข้ารหัสค่าตั้งผูกกับรหัสฐานข้อมูล

หากไม่ได้ตั้ง `SETTINGS_SECRET` กุญแจที่ใช้เข้ารหัส API key จะอนุมานจาก `DATABASE_URL` การหมุนรหัสฐานข้อมูลจึงทำให้คีย์ที่เก็บไว้ถอดไม่ออกและต้องกรอกใหม่

**ไม่ใช่ช่องโหว่ด้านความปลอดภัย** แต่เป็นความเสี่ยงด้านการใช้งาน แก้ได้โดยตั้ง `SETTINGS_SECRET` เป็นค่าคงที่แยกต่างหาก

---

## 5. มาตรการที่มีอยู่แล้วและผ่านการยืนยัน

| มาตรการ | รายละเอียด | ยืนยันโดย |
|---|---|---|
| ป้องกัน SQL injection โดยโครงสร้าง | ใช้ Prisma ORM ทั้งระบบ ไม่มี raw SQL | C1, C3 |
| ป้องกัน XSS โดยโครงสร้าง | React หนีอักขระอัตโนมัติ ไม่ใช้ `dangerouslySetInnerHTML` | C2 |
| ตรวจคำตอบที่ฝั่งเซิร์ฟเวอร์ | ไคลเอนต์ส่งผลการตรวจมาเองไม่ได้ | B1 |
| ยอดเงินอ่านจากฐานข้อมูล | QR สร้างจากราคาในระบบ ไม่ใช่จากคำขอ | B3 |
| เนื้อหาถูกล็อกตามสถานะการชำระเงิน | ส่ง URL วิดีโอเฉพาะผู้ที่ได้รับอนุมัติหรือผู้ดูแล | B4 |
| API key เข้ารหัสก่อนเก็บ | AES-256-GCM ตรวจจับการแก้ไขได้ | D2, D3 |
| ไม่ส่งคีย์กลับไปที่เบราว์เซอร์ | หน้าเว็บเห็นเฉพาะรูปแบบปิดบัง | D3 |
| คุกกี้เป็น httpOnly | สคริปต์ฝั่งไคลเอนต์อ่านไม่ได้ | ตรวจโค้ด |
| ตัวแปรสภาพแวดล้อมไม่อยู่ใน git | `.env`, `.env.production` ถูกกันไว้ | D1 |

---

## 6. ขั้นตอนที่แนะนำก่อน deploy ทุกครั้ง

```bash
npx tsc --noEmit                      # 1. ตรวจชนิดข้อมูล
npm run build                         # 2. ต้องคอมไพล์ผ่าน
node scripts/security-test.mjs        # 3. ต้องผ่านทุกกรณีวิกฤต
node scripts/package-source.mjs       # 4. ตัวสแกนความลับต้องไม่พบอะไร
```

ขั้นที่ 3 และ 4 คืนค่า exit code ที่ไม่ใช่ศูนย์เมื่อพบปัญหา จึงนำไปต่อกับ CI ให้บล็อกการ deploy อัตโนมัติได้

---

## 7. สิ่งที่อยู่นอกขอบเขตการทดสอบนี้

รายงานไว้เพื่อความครบถ้วน — สิ่งเหล่านี้**ยังไม่ได้ทดสอบ**

- การทดสอบเจาะระบบโดยผู้เชี่ยวชาญภายนอก
- ความปลอดภัยของโครงสร้างพื้นฐานของ Vercel และ TiDB Cloud (อยู่ในความรับผิดชอบของผู้ให้บริการ)
- การทดสอบภาระงานหนักและการปฏิเสธการให้บริการ (DoS)
- การตรวจสอบช่องโหว่ของไลบรารีที่พึ่งพาอย่างต่อเนื่อง — ควรเพิ่ม `npm audit` เข้าไปในขั้นตอนก่อน deploy
- การทดสอบความปลอดภัยของขั้นตอนการชำระเงินจริงกับธนาคาร (ระบบใช้การตรวจสอบด้วยคนเป็นด่านสุดท้าย)

---

## ภาคผนวก — ผลการทดสอบดิบ

ไฟล์ `security-test-results.json` บันทึกผลของการรันครั้งล่าสุดในรูป JSON ประกอบด้วยรหัสกรณีทดสอบ หมวด ผลผ่าน/ไม่ผ่าน ระดับความรุนแรง และรายละเอียดเมื่อไม่ผ่าน ใช้แนบเป็นหลักฐานหรือนำไปเปรียบเทียบระหว่างรอบการทดสอบได้
